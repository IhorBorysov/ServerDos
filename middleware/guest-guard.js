module.exports = function(req, res, next) {
    // 💡 Якщо користувач АВТОРИЗОВАНИЙ (умова інвертована)
    if (req.session.isAuthenticated) {
        // Перенаправляємо його з '/login' на головну сторінку
        return res.redirect('/'); 
    }
    
    // Якщо користувач НЕ авторизований, дозволяємо йому продовжити (до сторінки входу/реєстрації)
    next();
}